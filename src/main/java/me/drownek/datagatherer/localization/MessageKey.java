package me.drownek.datagatherer.localization;

public enum MessageKey {
    CONFIRM_MESSAGE,
    TIMEOUT_MESSAGE,
    ALREADY_IN_GATHERER,
    CANCELLED,
    CURRENT_STEP_INFO,
    VALUE_SET,
    SUCCESS
}